define(['react'], function(React) {
    var Dashboard = React.createClass({
        render: function () {
            return (
                <div>{'This is the dasboard.'}</div>
            );
        }
    });

    return Dashboard;
});